/*
题目描述
读入一个小于等于 444 的非负整数, 输出该数字对应的英文.

输入格式
一个非负整数.

输出格式
一串字符串, 代表英文单词. (没有首字母大写)

样例
样例输入 1
0
样例输出 1
zero
*/
#include<iostream>
using namespace std;
int main(void)
{
	int num;
	cin>>num;
	if(num==0)
		cout<<"zero";
	else if(num==1)
		cout<<"one";
	else if(num==2)
		cout<<"two";
	else if(num==3)
		cout<<"three";
	else if(num==4)
		cout<<"four";
	return 0;
}
